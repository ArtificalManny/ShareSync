// src/components/adaptive/BreakReminder.jsx
// ═══════════════════════════════════════════════════════════════════════════════
// ALIVE AWARE: Break Reminder Component (SAFELY DISABLED)
// Returns null to remove the UI without breaking downstream imports.
// ═══════════════════════════════════════════════════════════════════════════════

import React from 'react';

export function BreakReminder({
  className = '',
  position = 'bottom-right',
  onBreakStart,
  onBreakEnd,
  onDismiss,
}) {
  return null; // UI safely vaporized
}

export function MiniBreakIndicator({ className = '' }) {
  return null; // UI safely vaporized
}

export default BreakReminder;
