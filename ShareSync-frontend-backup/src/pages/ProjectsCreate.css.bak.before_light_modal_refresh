/* ═══════════════════════════════════════════════════════════════════════════════
   PROJECTS CREATE MODAL — Dark Form Overrides
   Scoped to .pc-create-modal to beat the "Gallery Walk" nuclear overrides
   in index.css that force white inputs globally.
   ═══════════════════════════════════════════════════════════════════════════════
*/

/* ── Beat [role="dialog"] > div { background-color: #FFFFFF } ── */
[role="dialog"] > .pc-create-modal {
  background-color: transparent !important;
}

/* ── Beat input[type="text"] { background-color: #ffffff !important } ──
   .pc-create-modal input[type="text"] is MORE specific than input[type="text"]
   so when both use !important, ours wins.
   ─────────────────────────────────────────────────────────────────────────── */
.pc-create-modal input[type="text"],
.pc-create-modal input[type="email"],
.pc-create-modal input[type="password"],
.pc-create-modal input:not([type="checkbox"]):not([type="radio"]),
.pc-create-modal select,
.pc-create-modal textarea {
  background-color: #0f172a !important;  /* slate-950 — deepest dark */
  background: #0f172a !important;        /* beat the shorthand too */
  color: #ffffff !important;              /* pure white text */
  border: 1px solid rgba(139, 92, 246, 0.3) !important; /* purple-500/30 */
  border-radius: 0.75rem !important;
  caret-color: #c084fc !important;        /* purple-400 caret */
  box-shadow: none !important;            /* kill the white box-shadow */
}

/* ── Placeholder text ── */
.pc-create-modal input::placeholder,
.pc-create-modal textarea::placeholder {
  color: #64748b !important;              /* slate-500 */
  opacity: 1 !important;
}

/* ── Select dropdown options ── */
.pc-create-modal select option {
  background-color: #0f172a !important;
  color: #ffffff !important;
}

/* ── Focus state — purple ring ── */
.pc-create-modal input[type="text"]:focus,
.pc-create-modal input[type="email"]:focus,
.pc-create-modal input[type="password"]:focus,
.pc-create-modal input:not([type="checkbox"]):not([type="radio"]):focus,
.pc-create-modal select:focus,
.pc-create-modal textarea:focus {
  background-color: #0f172a !important;
  background: #0f172a !important;
  border-color: #8b5cf6 !important;       /* purple-500 */
  box-shadow: 0 0 0 2px rgba(139, 92, 246, 0.5) !important;
}

/* ── Disabled state ── */
.pc-create-modal input:disabled,
.pc-create-modal select:disabled,
.pc-create-modal textarea:disabled {
  opacity: 0.5 !important;
  cursor: not-allowed !important;
}

/* ── Autofill override (beat the global webkit autofill) ── */
.pc-create-modal input:-webkit-autofill,
.pc-create-modal input:-webkit-autofill:hover,
.pc-create-modal input:-webkit-autofill:focus,
.pc-create-modal input:-webkit-autofill:active {
  -webkit-box-shadow: 0 0 0 30px #0f172a inset !important;
  -webkit-text-fill-color: #ffffff !important;
  background-color: #0f172a !important;
  caret-color: #c084fc !important;
}
