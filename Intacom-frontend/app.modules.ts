//..other imports
import { AppGateway } from "./Src/app.gateway";

@Module({
    imports: [
        //...existing imports
    ],
    controllers: [],
    providers: [AppGateway]
})
export class AppModule