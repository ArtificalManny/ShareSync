import { Module } from '@nestjs/common';

/**
 * Temporary MessagesModule stub.
 *
 * app.module.ts imports ./messages/messages.module.
 * This keeps the backend bootable until the full messages feature is restored.
 */
@Module({})
export class MessagesModule {}
