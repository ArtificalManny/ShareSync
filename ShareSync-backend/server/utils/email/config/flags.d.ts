import type { ScoreWeights } from "../discovery/score";
export declare const DISCOVERY_MAX_LIMIT: number;
export declare const DEFAULT_DISCOVERY_WEIGHTS: ScoreWeights;
export declare const DISCOVERY_ENABLED: boolean;
export declare const FLAGS: {
    DISCOVERY_ENABLED: boolean;
    DISCOVERY_MAX_LIMIT: number;
    DEFAULT_DISCOVERY_WEIGHTS: ScoreWeights;
};
export default FLAGS;
